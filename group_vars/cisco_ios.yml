# Lab IOS if a host is uncommented in [cisco_ios]. Same env as arista_lab.
ansible_user: "{{ lookup('env', 'ANSIBLE_USER_LAB') }}"
ansible_password: "{{ lookup('env', 'ANSIBLE_PASSWORD_LAB') }}"
ansible_become_password: "{{ lookup('env', 'ANSIBLE_BECOME_PASSWORD_LAB') | default(omit, true) }}"
