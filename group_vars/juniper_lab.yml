# Lab vMX NETCONF. Empty JUNOS_* falls back to lab Arista env (ANSIBLE_*_LAB).
ansible_user: "{{ lookup('env', 'JUNOS_USER') | default(lookup('env', 'ANSIBLE_USER_LAB'), true) }}"
ansible_password: "{{ lookup('env', 'JUNOS_PASSWORD') | default(lookup('env', 'ANSIBLE_PASSWORD_LAB'), true) }}"
