# Lab device credentials (SSH / enable). Used by group_vars via lookup('env').
ANSIBLE_USER=
ANSIBLE_PASSWORD=
ANSIBLE_BECOME_PASSWORD=

# Used when the Batfish service is added to Compose later.
BATFISH_HOST=127.0.0.1
BATFISH_PORT=9996
